<% if @suppliers.empty? %>
  <p>Não existem fornecedores cadastrados</p>
<% end %>
<h2>Fornecedores</h2>
<% @suppliers.each do | supplier | %>
  <div>
    <h3><%= link_to supplier.corporate_name, supplier_path(supplier.id)  %></h3>
    <dl>
      <dt>Nome fantasia: </dt>
      <dd><%= supplier.brand_name%></dd>
    <dl>
  </div>
<% end %>
<div>
  <%= link_to 'Voltar', root_path %>
</div>